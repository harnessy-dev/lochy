{
  "name": "sessionport",
  "version": "0.1.0",
  "description": "Save and restore agent coding sessions across machines",
  "type": "module",
  "license": "MIT",
  "bin": {
    "sessionport": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.699.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
